name: Flask CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure Docker for Minikube
        run: |
          eval $(minikube -p minikube docker-env)

      - name: Set image tag
        id: vars
        run: echo "IMAGE_TAG=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - name: Build Docker image
        run: |
          docker build -t flask-app:${{ env.IMAGE_TAG }} .

      # ✅ Run tests inside Docker
      - name: Run Tests
        run: |
          docker run --rm flask-app:${{ env.IMAGE_TAG }} pytest tests/

      - name: Apply Kubernetes manifests
        run: |
          kubectl apply -f flask-deployment.yaml
          kubectl apply -f flask-service.yaml

      - name: Update Kubernetes deployment
        run: |
          kubectl set image deployment/flask-app-deployment flask-app=flask-app:${{ env.IMAGE_TAG }}
          kubectl rollout status deployment/flask-app-deployment

      - name: Get Pods
        run: |
          kubectl get pods

